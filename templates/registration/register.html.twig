{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}



{% block body %}
    <style>
        /*body{
            background-color:#000000;
            width:100wh;
            height:100vh;
            display: flex;
            justify-content: center;
            flex-direction: column;     
        }

        h1{
            font-size:40px;
            margin-top:-80px;
            padding-bottom:80px;
            margin-left:auto;
            margin-right:auto;
        }

        .regForm{
            background-color: #000000;
            
            width:700px;
            height:500px;
            color:#dddddd;

            border: 4px solid #dddddd;
            border-radius:7px;

            margin: 0 auto;

            display: flex;
            justify-content: center;
            flex-direction: column;

            position:relative;
        }

        .homeButt{
            position:absolute;
            bottom:0;
            right:0;
            margin-right:10px;
            margin-bottom:10px;
        }*/

    </style>

    <div class = "regForm">
        <h1>Регитрация</h1>

        {{ form_start(registrationForm) }}
            {{ form_row(registrationForm.username, {
                label: 'Имя'
            }) }}
            {{ form_row(registrationForm.lastname, {
                label: 'Псевдоним'
            }) }}
            {{ form_row(registrationForm.plainPassword, {
                label: 'Пароль'
            }) }}
            {{ form_row(registrationForm.agreeTerms, {
                label: 'Согласиться с условиями '
            }) }}
            <button type = "submit" class="btn">Создать аккаунт</button>
        {{ form_end(registrationForm) }}

        <button class = "homeButt" onclick= "document.location='{{ path('home') }}'" style = "margin-top:50px;">На домашнюю</button>
    </div>
{% endblock %}
