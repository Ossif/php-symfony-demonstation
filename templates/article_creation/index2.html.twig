{% extends 'base.html.twig' %}

{% block title %}Hello DivisionController!{% endblock %}

{% block body %}

<script>
    $('#id1').click(function(){
	    $('#hiddenForm').show();  
        $('#id1').hide();         
	});
</script>

<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }

    .rightPart{

    }

    .avatar{
        width:40px;
        height:40px;

        border: 2px solid #dddddd;
        border-radius:50%;

    }

    .mainPart{
        width:100wh;
        min-height:80vh;
        background-color:#eeeeee;
    }

    .formName{
        min-height:60px;
        width:100%;
        border-radius:10px;
        font-size:30px;
        text-align:center;
    }

    .formText{
        width:100%;
        min-height:50vh;
        border-radius:10px;
        font-size:20px;
    }
    
    .btn{
         background-color:#0d6efd;
         border-radius:8px;
         color:white;
         font-size:20px;
    }

</style>

<div class="container">
    <header class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4 border-bottom">
        <a href="/" class="d-flex align-items-center col-md-3 mb-2 mb-md-0 text-dark text-decoration-none">
            <img src="{{ asset('Files/Icons/home.svg') }}" alt="Home">
        </a>

        {% if isAunt == null%}
            <div class="col-md-3 text-end">
            <button onclick= "document.location='{{ path('login') }}'" type="button" class="btn btn-outline-primary me-2">Login</button>
            <button onclick= "document.location='{{ path('register') }}'" type="button" class="btn btn-primary">Sign-up</button>
            </div>
        {% else %}
            <div style = "display:row; min-width:230px;">
                <button onclick = "document.location='{{ path('app_logout') }}'"  type="button" class="btn btn-primary"}}'">Выход</button>
                <div class = "avatar" style = "float: right;"></div>
            </div>
        {% endif %}
    </header>
</div>
<div class = "mainPart">
    <div style = "height:10px;"></div>
    <div style = "width:80%; hieght:100%; margin: 0 10%;">
    {{ form_start(articleCreationForm) }}
        {{ form_row(articleCreationForm.name, {'attr': {'class': 'formName'}}) }}
        <div style = "height:10px;"></div>
        {{ form_row(articleCreationForm.text, {'attr': {'class': 'formText'}}) }}
        <div style = "height:10px;"></div>
        <button type="submit" class="btn" >Сохранить</button>
    {{ form_end(articleCreationForm) }}
    </div>
</div>
{% endblock %}
